[project]
name = "multisubs"
version = "1.0.0"
description = "A tool to generate and embed subtitles into videos."
dependencies = [
    "openai-whisper",
    "ffmpeg-python",
]

[project.scripts]
multisubs = "multisubs.cli:main"
